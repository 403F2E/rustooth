# Rustooth
A small project simulating a remote controller for a pc, it uses flutter for the phone app (the remote controller UI) and Rust (BlueR, tokio and rdev crates) as the server.

# Prerequirement
- Mobile : flutter, Android SDK and
